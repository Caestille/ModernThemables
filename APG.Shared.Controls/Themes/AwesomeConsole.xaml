<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:APG.Shared">

    <Style x:Key="AwesomeConsoleStyle" TargetType="{x:Type local:AwesomeConsole}">
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:AwesomeConsole}">
                    <Border BorderThickness="1" BorderBrush="DimGray">
                        <ScrollViewer>
                            <ItemsControl x:Name="ItemsControl" FontFamily="Consolas" />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:AwesomeConsole}" BasedOn="{StaticResource AwesomeConsoleStyle}" />

</ResourceDictionary>
