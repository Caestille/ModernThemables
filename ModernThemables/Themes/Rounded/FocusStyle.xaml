<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="RoundedFocusStyleControlTemplate">
        <Grid>
            <Border
                Margin="-3"
                Background="{DynamicResource SecondaryTextBrush}"
                CornerRadius="7"
                Opacity="0.2"
                SnapsToDevicePixels="true" />
            <Border
                Margin="-3"
                Background="Transparent"
                BorderBrush="{DynamicResource SecondaryTextBrush}"
                BorderThickness="1"
                CornerRadius="7"
                SnapsToDevicePixels="true" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="FocusStyle">
        <Setter Property="Control.Template" Value="{StaticResource RoundedFocusStyleControlTemplate}" />
    </Style>

</ResourceDictionary>
