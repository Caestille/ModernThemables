<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animatedicons="clr-namespace:ModernThemables.Icons.AnimatedIcons;assembly=ModernThemables.Icons"
    xmlns:ch="clr-namespace:CoreUtilities.Helpers.WPF;assembly=CoreUtilities"
    xmlns:controls="clr-namespace:ModernThemables.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:scalableicons="clr-namespace:ModernThemables.Icons;assembly=ModernThemables.Icons"
    mc:Ignorable="d">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/ModernThemables;component/Themes/Rounded/FocusStyle.xaml" />
        <ResourceDictionary Source="../Converters.xaml" />
        <ResourceDictionary Source="/CoreUtilities;component/Themes/Converters.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Storyboard x:Key="MenuOpenAnimation">
        <DoubleAnimation
            AutoReverse="False"
            Storyboard.TargetProperty="Width"
            To="220"
            Duration="0:0:0.2">
            <DoubleAnimation.EasingFunction>
                <CircleEase />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="MenuCloseAnimation">
        <DoubleAnimation
            AutoReverse="False"
            Storyboard.TargetProperty="Width"
            To="51"
            Duration="0:0:0.2">
            <DoubleAnimation.EasingFunction>
                <CircleEase />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Style
        x:Key="MenuButton"
        BasedOn="{StaticResource ResourceKey={x:Type controls:Button2}}"
        TargetType="controls:Button2">
        <Setter Property="Padding" Value="2" />
        <Setter Property="MouseOverBrush" Value="Transparent" />
        <Setter Property="MouseDownBrush" Value="Transparent" />
    </Style>

    <ControlTemplate x:Key="RoundedMenuControlTemplate" TargetType="{x:Type controls:HamburgerMenu}">
        <ControlTemplate.Resources />
        <Grid>
            <Border
                Width="51"
                HorizontalAlignment="Left"
                Background="{TemplateBinding Background}"
                BorderThickness="1"
                CornerRadius="0,10,10,0">
                <Border.Style>
                    <Style>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMenuOpen, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard Storyboard="{StaticResource MenuOpenAnimation}" />
                                </DataTrigger.EnterActions>
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard Storyboard="{StaticResource MenuCloseAnimation}" />
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Border
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="0,10,10,0">
                        <Border.Visibility>
                            <MultiBinding Converter="{StaticResource MultiBoolAndToVisibilityConverter}">
                                <Binding Path="IsMenuOpen" RelativeSource="{RelativeSource TemplatedParent}" />
                                <Binding
                                    Converter="{StaticResource BoolInverter}"
                                    Path="IsMenuPinned"
                                    RelativeSource="{RelativeSource TemplatedParent}" />
                            </MultiBinding>
                        </Border.Visibility>
                        <Border.Background>
                            <LinearGradientBrush Opacity="0.15">
                                <GradientStop Offset="0" Color="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=AccentBrush, Converter={StaticResource BrushToColourConverter}}" />
                                <GradientStop Offset="1" Color="Transparent" />
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                    <controls:BlurHost
                        BlurBackground="{Binding BlurBackground, RelativeSource={RelativeSource TemplatedParent}}"
                        BlurEnabled="{TemplateBinding IsMenuPinned,
                                                      Converter={StaticResource BoolInverter}}"
                        BlurOpacity="0.5"
                        Focusable="False"
                        RedrawTrigger="{Binding RelativeSource={RelativeSource Self}, Path=ActualWidth}">
                        <Grid Margin="0,12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid Margin="9,0,0,0" VerticalAlignment="Top">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <controls:Button2
                                    x:Name="PART_OpenButton"
                                    Width="30"
                                    Height="30"
                                    Padding="2"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Top"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    CornerRadius="5"
                                    Foreground="{TemplateBinding Foreground}"
                                    Style="{StaticResource MenuButton}">
                                    <controls:Button2.Visibility>
                                        <MultiBinding Converter="{StaticResource MultiBoolOrToVisibilityConverter}">
                                            <Binding
                                                Converter="{StaticResource BoolInverter}"
                                                Path="IsMenuPinned"
                                                RelativeSource="{RelativeSource TemplatedParent}" />
                                            <Binding
                                                Converter="{StaticResource BoolInverter}"
                                                Path="IsMenuOpen"
                                                RelativeSource="{RelativeSource TemplatedParent}" />
                                        </MultiBinding>
                                    </controls:Button2.Visibility>
                                    <Grid Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:Button2}}, Path=ActualWidth}" Height="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:Button2}}, Path=ActualHeight, Converter={StaticResource ValueAdderConverter}, ConverterParameter=-3}">
                                        <Grid.Background>
                                            <SolidColorBrush Opacity="0.01" Color="Black" />
                                        </Grid.Background>
                                        <Grid.Style>
                                            <Style TargetType="{x:Type Grid}">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="False">
                                                        <Setter Property="Opacity" Value="0.5" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <animatedicons:AnimatedMenuIcon Margin="1,3,6,3" IsOpen="{TemplateBinding IsMenuOpen}" />
                                    </Grid>
                                </controls:Button2>
                                <controls:Button2
                                    x:Name="PART_PinButton"
                                    Grid.Column="1"
                                    Width="30"
                                    Height="30"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Top"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    CornerRadius="5"
                                    Foreground="{TemplateBinding Foreground}"
                                    Style="{StaticResource MenuButton}">
                                    <Grid
                                        Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:Button2}}, Path=ActualWidth}"
                                        Height="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:Button2}}, Path=ActualHeight}"
                                        Margin="0,-1,0,0">
                                        <Grid.Background>
                                            <SolidColorBrush Opacity="0.01" Color="Black" />
                                        </Grid.Background>
                                        <Grid.Style>
                                            <Style TargetType="{x:Type Grid}">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="False">
                                                        <Setter Property="Opacity" Value="0.5" />
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding IsMenuOpen, RelativeSource={RelativeSource TemplatedParent}}" Value="False">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <scalableicons:Icon Margin="5" Kind="Pin" />
                                    </Grid>
                                </controls:Button2>
                                <controls:SearchBox
                                    x:Name="PART_SearchBox"
                                    Grid.Column="2"
                                    Height="20"
                                    Margin="8,6,10,4"
                                    VerticalAlignment="Top"
                                    CornerRadius="5"
                                    Foreground="{TemplateBinding Foreground}"
                                    Visibility="{TemplateBinding IsMenuOpen,
                                                                 Converter={StaticResource VisibleIfTrueConverter}}">
                                    <controls:SearchBox.Background>
                                        <SolidColorBrush Opacity="0.1" Color="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Foreground, Converter={StaticResource BrushToColourConverter}}" />
                                    </controls:SearchBox.Background>
                                </controls:SearchBox>
                            </Grid>
                            <Border
                                Grid.Row="1"
                                Height="1"
                                Margin="10,12,10,0"
                                VerticalAlignment="Top"
                                Background="{TemplateBinding Foreground}"
                                Opacity="0.1" />
                            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                                <Grid>
                                    <ItemsControl
                                        x:Name="ItemsControl"
                                        Margin="5,2"
                                        Focusable="False"
                                        ItemTemplate="{Binding ItemTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                        ItemsSource="{Binding Items, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}"
                                        Visibility="{Binding ElementName=ItemsControlSearch, Path=Visibility, Converter={StaticResource VisibilityInverterConverter}}" />
                                    <ItemsControl
                                        x:Name="ItemsControlSearch"
                                        Height="Auto"
                                        Margin="5,2"
                                        VerticalAlignment="Top"
                                        Focusable="False"
                                        ItemTemplate="{Binding SearchItemTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                        ItemsSource="{Binding FilteredItems, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}">
                                        <ItemsControl.Visibility>
                                            <MultiBinding Converter="{StaticResource MultiBoolAndToVisibilityConverter}">
                                                <Binding
                                                    Converter="{StaticResource StringNotEmptyConverter}"
                                                    Path="SearchText"
                                                    RelativeSource="{RelativeSource TemplatedParent}" />
                                                <Binding
                                                    Converter="{StaticResource StringIsNotValueConverter}"
                                                    ConverterParameter="Search"
                                                    Path="SearchText"
                                                    RelativeSource="{RelativeSource TemplatedParent}" />
                                            </MultiBinding>
                                        </ItemsControl.Visibility>
                                    </ItemsControl>
                                    <TextBlock
                                        Margin="12"
                                        Foreground="{TemplateBinding Foreground}"
                                        Text="No Results">
                                        <TextBlock.Visibility>
                                            <MultiBinding Converter="{StaticResource MultiBoolAndToVisibilityConverter}">
                                                <Binding
                                                    Converter="{StaticResource SequenceEmptyBooleanConverter}"
                                                    Path="FilteredItems"
                                                    RelativeSource="{RelativeSource TemplatedParent}" />
                                                <Binding
                                                    Converter="{StaticResource StringNotEmptyConverter}"
                                                    Path="SearchText"
                                                    RelativeSource="{RelativeSource TemplatedParent}" />
                                            </MultiBinding>
                                        </TextBlock.Visibility>
                                    </TextBlock>
                                </Grid>
                            </ScrollViewer>
                            <Grid Grid.Row="3">
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <controls:HamburgerMenuItem
                                    Title="Settings"
                                    Margin="5,2"
                                    HorizontalAlignment="Stretch"
                                    AccentBrush="Transparent"
                                    CanAddChild="False"
                                    CanDelete="False"
                                    SelectionMode="Expand"
                                    IsContainingMenuOpen="{Binding IsMenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                    IsExpanded="{Binding ShowSettings, RelativeSource={RelativeSource TemplatedParent}}"
                                    SelectCommand="{Binding ShowSettingsCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                    Visibility="{Binding SettingsVm, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource VisibleIfNotNullConverter}}">
                                    <controls:HamburgerMenuItem.IconTemplate>
                                        <DataTemplate>
                                            <scalableicons:Icon
                                                Width="15"
                                                Foreground="{DynamicResource PrimaryControlMouseOverBrush}"
                                                Kind="Gear" />
                                        </DataTemplate>
                                    </controls:HamburgerMenuItem.IconTemplate>
                                </controls:HamburgerMenuItem>
                                <ContentControl
                                    Grid.Row="1"
                                    Margin="9,2"
                                    Content="{Binding SettingsVm, RelativeSource={RelativeSource TemplatedParent}}"
                                    ContentTemplate="{Binding SettingsTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                    Visibility="{Binding ShowSettings, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource VisibleIfTrueConverter}}" />
                                <Border
                                    Grid.RowSpan="2"
                                    Width="3"
                                    Margin="1,10"
                                    HorizontalAlignment="Left"
                                    Background="{TemplateBinding AccentBrush}"
                                    CornerRadius="1.5"
                                    Visibility="{Binding ShowSettings, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource VisibleIfTrueConverter}}" />
                            </Grid>
                        </Grid>
                    </controls:BlurHost>
                </Grid>
            </Border>
        </Grid>
    </ControlTemplate>

    <Style TargetType="{x:Type controls:HamburgerMenu}">
        <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource SecondaryBackgroundBrush}" />
        <Setter Property="AccentBrush" Value="{DynamicResource ThemeMouseDownBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusStyle}" />
        <Setter Property="Template" Value="{StaticResource RoundedMenuControlTemplate}" />
        <Setter Property="SettingsVm" Value="Add a viewmodel to the SettingsVm property, and a DataTemplate to the SettingsTemplate property to display your application settings here" />
        <Setter Property="SettingsTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border
                        Margin="10,0"
                        Padding="10"
                        Background="{DynamicResource SecondaryBackgroundBrush}"
                        BorderBrush="{DynamicResource SecondaryControlBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="10">
                        <TextBlock
                            Foreground="{DynamicResource SecondaryTextBrush}"
                            Text="{Binding}"
                            TextWrapping="WrapWithOverflow" />
                    </Border>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <controls:HamburgerMenuItem
                        Title="{Binding Name}"
                        Margin="5,2"
                        AccentBrush="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:HamburgerMenu}}, Path=AccentBrush}"
                        CanAddChild="False"
                        CanDelete="False"
                        SelectionMode="Automatic"
                        Foreground="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:HamburgerMenu}}, Path=Foreground}"
                        IsContainingMenuOpen="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:HamburgerMenu}}, Path=IsMenuOpen}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>