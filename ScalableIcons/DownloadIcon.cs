using System.Windows;

namespace ModernThemables.ScalableIcons
{
	public class DownloadIcon : BaseIcon
	{
		static DownloadIcon()
		{
			DefaultStyleKeyProperty.OverrideMetadata(typeof(DownloadIcon), new FrameworkPropertyMetadata(typeof(DownloadIcon)));
		}
	}
}
