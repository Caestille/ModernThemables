namespace ModernThemables.Charting.Models
{
    /// <summary>
    /// Enum describing where a tooltip will be placed on a chart as the user moves the cursor.
    /// </summary>
    public enum TooltipLocation
    {
        /// <summary>
        /// Tooltip is by the user cursor.
        /// </summary>
        Cursor,

        /// <summary>
        /// Tooltip is by its respective point.
        /// </summary>
        Points,
    }
}
